# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Go
/user-service/server
/post-service/server
/bff/server
*.exe
*.test
*.out
go.work

# Node.js / Frontend
node_modules/
npm-debug.log
yarn-error.log
/frontend/dist/
/frontend/coverage/
.env.local
.env.development.local
.env.test.local
.env.production.local

# Terraform / K8s (if any local secrets)
.terraform/
*.tfstate
*.tfstate.backup

# Proto generated files (optional: usually we commit them, but if we want to ignore)
# For this project, we probably want to commit them as they are part of the source.
